{
  "name": "smux-monorepo",
  "private": true,
  "type": "module",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "build": "pnpm -r --parallel run build",
    "build:app": "pnpm --filter=react-app run build",
    "check": "pnpm -r --parallel run check",
    "dev": "pnpm --filter=react-app run dev",
    "test": " pnpm --filter=smux run test",
    "test:coverage": " pnpm --filter=smux run test:coverage"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "packageManager": "pnpm@10.17.1",
  "pnpm": {
    "overrides": {
      "vite": "npm:rolldown-vite@7.1.12"
    }
  }
}
